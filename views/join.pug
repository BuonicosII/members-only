extends layout

block content
  h1= title
  form(method='POST') 
    input#secretPassword(type='password', name='secretPassword' required)
    button(type='submit') Submit

  if errors
    ul
      for error in errors
          li!= error.msg 