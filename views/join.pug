extends layout

block content
  div
    h1= title
    form(method='POST')
      div.formFields
        label(for="secretPassword") Enter the secret password
        input#secretPassword(type='password', name='secretPassword' required)
      button(type='submit') Submit

    if errors
      ul
        for error in errors
            li!= error.msg 